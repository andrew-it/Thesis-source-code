enum class EXPR_TYPES {
    BODY,
    RET_TYPE,
    FUNC_NAME,
    SIGNATURE,
    PARAMS,
    VALUE,
    VAR_NAME,
    LIB,
    CONDITION
}

enum class PATTERN_TYPES {
    VAL_STR,
    VAL_INT,
    VAR_CHAR,
    LOOP,
    IF_CONDITION,
    ELSE_CONDITION,
    ELIF_CONDITION,
    TERNARY,
    FUNC_DECL,
    ENTRY_POINT,
    FUNC_CALL,
    VAR_DECL,
    VAR_ASSIGNMENT,
    EMPTY_OPERATOR,
    DEPENDENCY,
    PARAMS
}

enum class STD_TYPES {
    STRING,
    INT,
    REAL
}

